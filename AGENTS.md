# AGENTS.md

## コードスタイル

- 変数名・関数名は英語、コメントは日本語とする。
- 共通処理は`lib/`へ分離し、CLIと対話UIの両方から再利用できるようにする。
- シェルスクリプトはBashを対象とし、`set -euo pipefail`を使用する。

## 公開範囲

- APK、XAPK、署名鍵、署名パスワード、解析資料をリポジトリへ含めない。
- 外部ツールは固定バージョンとSHA-256を記録し、セットアップ時に取得する。
- ユーザーの署名鍵は端末内に生成し、リポジトリやログへ出力しない。

## ドキュメント

- 設計、確認済み事項、推定事項、Android側の制約は`docs/`へ記録する。
- 日本語を含む`.md`はBOM付きUTF-8で保存する。
