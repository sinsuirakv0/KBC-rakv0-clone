# セキュリティ

## 署名鍵

署名鍵は端末内で生成され、GitHubへ送信されません。`~/.local/share/kbc-clone/keystore/`を公開リポジトリ、共有ストレージ、チャットへアップロードしないでください。

## 外部ツール

APKEditorは固定バージョンを取得し、SHA-256を検証します。不一致の場合は処理を中止します。

## 報告

署名鍵の漏えい、任意コマンド実行、意図しないファイル削除につながる問題は、再現に必要な最小限の情報だけを添えて非公開で報告してください。APK、XAPK、署名鍵、セーブデータを添付しないでください。
